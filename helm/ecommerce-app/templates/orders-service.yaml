apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ordersService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.ordersService.name }}
    {{- include "ecommerce-app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.ordersService.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.ordersService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.ordersService.name }}
    spec:
      {{- if .Values.global.imagePullSecretName }}
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecretName }}
      {{- end }}
      containers:
      - name: {{ .Values.ordersService.name }}
        image: "{{ .Values.ordersService.image.repository }}:{{ .Values.ordersService.image.tag }}"
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.ordersService.port }}
          name: http
        env:
        - name: PORT
          value: "{{ .Values.ordersService.port }}"
        - name: CORS_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: CORS_ORIGINS
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: MONGODB_URI
        livenessProbe:
          tcpSocket:
            port: {{ .Values.ordersService.port }}
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: {{ .Values.ordersService.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.ordersService.resources | nindent 10 }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ordersService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.ordersService.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.ordersService.name }}
  ports:
  - port: {{ .Values.ordersService.port }}
    targetPort: {{ .Values.ordersService.port }}
    name: http
